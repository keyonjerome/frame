{
  "name": "frame-web",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Flask video library & JS tests for utilities",
  "license": "MIT",
  "scripts": {
    "test": "jest --colors"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/js/**/*.test.js"],
    "verbose": true
  }
}